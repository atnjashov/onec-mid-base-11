
#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)

	//Атняшов В. А.: добавить ДействуетНаОсновании
	СтандартнаяОбработка = Ложь;
	ДействуетНаОсновании = Данные.Ссылка.ДействуетНаОсновании;
	ФИО = Данные.Ссылка.ФИО;
	Если ПустаяСтрока(ДействуетНаОсновании) Тогда
		Представление = ФИО;
	Иначе	
		Представление = СтрШаблон("%1 на осн. %2", ФИО, ДействуетНаОсновании);
	КонецЕсли;
	//
	
КонецПроцедуры

#КонецОбласти
